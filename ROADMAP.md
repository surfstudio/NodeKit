# CoreNetKit манифест

Здесь описан мой взгляд на то, что сейчас представляет из себя билиотека и планы ее развития. 

**Оглавление**

- [CoreNetKit манифест](#corenetkit)
    - [Now](#now)
    - [Contexts](#contexts)
    - [Core.Adapters](#coreadapters)
        - [Cache](#cache)
        - [Error](#error)
    - [BaseServerRequest](#baseserverrequest)
    - [ReusablePagingRequest](#reusablepagingrequest)
    - [BaseServerError](#baseservererror)
    - [BaseResult](#baseresult)
    - [ServerPart](#serverpart)
        - [Protocols.CoreServerRequest](#protocolscoreserverrequest)
        - [Protocols.CoreServerResponse](#protocolscoreserverresponse)
    - [BaseCoreServerResponse](#basecoreserverresponse)
    - [BaseCoreServerRequest](#basecoreserverrequest)
    - [ResponseResult](#responseresult)
    - [TokenProvider](#tokenprovider)
    - [AccessSafeContextManager](#accesssafecontextmanager)
    - [Pagination](#pagination)
- [Roadmap](#roadmap)
    - [Modularization](#modularization)
    - [More features](#more-features)
    - [Debugging and testing](#debugging-and-testing)
- [Checklist](#checklist)

## Now

В данный момент в качестве либы для сетевого взаимодействия используется `Alamofire🔥`. Это одновременно и проблема и нет, по-скольку в ней реализованы разные типы кодирования и хорошо отлажена вся 'оберточная' часть, однако с другой стороны - там много лишнего. 

**Обозначения**
- 🔥 - Зависит от `Alamofire`
- 💥 - Необходимо декомпозировать
- ☠️ - Нужно удалить
- 💩 - **ОООЧЕНЬ** плохой код. Требуется рефакторинг
- ⚙️ - требует переработка/доработка


Библиотека поделена на несколько модулей:
- Context [⚙️ ] - cодержит реализацию и протоколы для контекстов
- Core
    - Adapters
        - Cache [⚙️ ] - адаптеры для кэширования запросов
        - Error [⚙️ ] - адаптеры для обработки ошибок
    - BaseServerRequest.swift [🔥 💥  ☠️ ] - высокоуровневая обертка над запросом в сеть. Перегружается под нужду разработчика. 
    - ReusablePagingRequest.swift [☠️ ] - просто протокол для реюза запроса
    - BaseServerError.swift [☠️ ] - `enum` для ошибок сети и кэша
    - BaseResult.swift [💩, ⚙️ ] - `enum` с сырым резльтатом ответа от сервера
    - ServerPart - тут лежит все, что связано с работой с сетью, кэшированием и так далее
        - Protocols [⚙️ ] - протоколы для сетевых запроса и ответа. 
        - BaseCoreServerResponse.swift [🔥 💥 ] - тут содержиться **ВСЯ** логика обработка сырого сетевого ответа и маппинга ошибок
        - BaseCoreServerRequest.swift [🔥 💥 💩 ] - тут содержится вся логика отвечающая за формирование запроса, применение политики кэширования, обработки ответа и логирвоания.
        - MultipartData.swift - просто структура данных для Multipart запроса
        - ResponseResult.swift [☠️ ] - результат для проброса ответа из `BaseCoreServerResponse` в `BaseServerRequest`
        - TokenProvider.swift [⚙️ ] - поставляет токен в запрос. 
- Kit - это та самая штука, которая содержит различные утилитки
    - Safe
         - AccessSafeContextManager.swift [⚙️ ] - менеджре для возобновления Access-токена (в случае OAuth2)
         - AccessSafeRequestManager.swift [☠️ ] - доконтекстный менеджер. Устарел.
    - Pagination [⚙️ ] - содержит контексты для пагинации. Были сведенья об mempry leak'ах.

## Contexts 

Здесь содержаться все контексты, которые могут использоваться для обработки ответов из сервера. 
В одном из проектов я написал несколько удобных `extension` для соединения контекстов в цепочку и некоторых операций вроде `map {}`, `filter {}`.
Это необходимо перенести сюда. 

При очень сложной логике работы с контекстами (например, при их шаринге и переиспользовании) могут возникать проблемы с отпиской. 
Например, контекст привязан к стриму. Контекст представляет из себя цепочки из нескольких контекстов, каждый изх которых щапоминаем последнее заэмичиное событие. 
1. Стрим падает с ошибкой
2. Происходит эмит ошибки
3. Первый контекст в цепочке запоминает ошибку и пробрасывает ее дальше
4. Второй контекст запоминает ошибку и пробрасывает ее дальше
5. Происходит автовозобновление подписки, так как мы смогли определить, что у нас появился интернет
6. Код внутри обработчика стрима затирает память контекста (у первого из цепочки), однако на второй из цепочки он не распространяется
7. Новый подписчик пытается подписаться на контекст
8. Подписчик сразу получает оишбку, так как во втором контексте она не затерлась

**Стоит обратить внимание на эту проблему**

Необхождимо перепроектировать структуру контекстов так, чтобы метод perform был доступен между контекстами.

## Core.Adapters

### Cache

В общем он неплохо написан, но стоит посмотреть внимательно на него и подумать о том, как уложится этот интерфей2с в модульную структуру библиотеки. И как он подойдет для того, чтобы написать какой-то нетривиальный кэш. 

К тому же, стоит подумать о том, что кэширование вообще (включая политики) стоит сделать отдельным, подклчюаемым модулем. Такм образом можно будет упростить код отправки/обработки запроса и однвоременно упростить логику для кэширования. 

### Error

Кажется, что тут и так все хорошо. 

## BaseServerRequest

Очень простой класс, который представляет из себя сетевой запрос высокого уровня. Это значит, что он позволяет низкоуровневой части либы сформировать запрос, а затем, передать ответ сервера вверх. 

Он такой простой, что аж противно. Стоит подумать о том, чтобы прелепить к нему модуль маппинга. 

Конечный вариант использования `BaseServerRequest` должен сводиться к тому, чтобы не нужно было его сабклассить. Просто типизировать через дженерики.

## ReusablePagingRequest

Нафиг он нужен? Хорошо бы, чтобы все реквесты были реюзбл (особенно учитывая, что в идеале у нас будет один реквест чтоб править всеми)

## BaseServerError

На самом деле - бесполезный кусок кода. Теперь. В Foundation теперь есть все коды ответов в одном `enum`. 

## BaseResult

Там написан плохой код. Оберка `switch-case` с опционалом. Нужно выпилить все это, возможно стоит подумать о том, как изменить case'ы в случае ответа из кэша

## ServerPart

### Protocols.CoreServerRequest

Это протокол для низкоуровневого запроса в сеть.

```Swift

public protocol CoreServerRequest {
    func perform(with completion: @escaping (CoreServerResponse) -> Void)
    func cancel()
}

```
Выглядит замечательно. Но чего-то тут нехватает. Например всяких стыков для разных модулей. Например для логгирования или кэширования или чего-нибудь еще.

### Protocols.CoreServerResponse

Протокол для низкоуровневого ответа от сети. Уже попахивает.

```Swift
public protocol CoreServerResponse {
    var isNotModified: Bool { get }
    var statusCode: Int { get }
    var isConnectionFailed: Bool { get }
    
    var result: ResponseResult<Any> { get set }
    var httpResponse: HTTPURLResponse? { get set }
}
```
Мне кажется, что он должен быть каким-то более в фуцнкциональным. 

Во всяком случае, он не должен сверкать своим `HTTPURLResponse` точно.

## BaseCoreServerResponse

Содержит замечательный комментарий:
```Swift
// kostil
// TODO: Think about it
```

Прямо в конструкторе делает сложные преобразования с ответом из сети. Стоит подумать о том как его почистить. 

## BaseCoreServerRequest

**praeterea, Deus non est**

Этот класс содержит в себе:
- Конструктор на 10 параметров
- Реализацию политик кэширования
- Создание запроса и обработку ответа
- Логирование
- Просто сложный код

Вобщем тут надо декомпозировать. Очень много и очень сильно. 
И постараться не умереть в процессе. 

## ResponseResult

Лишняя сущность.

## TokenProvider

Намек на то, что нужно модуляризировать архитектуру либы. 

## AccessSafeContextManager

Оно вроде работает. Но надо тестить. Вроде бы на MDK используют. 

## Pagination
Течет память.
Тут не очень понятно что за что отвечает. 
Структура контекстов не ясна и не очень детально проработана.
Нужно сделать по-нормальному.

# Roadmap

Проанализировав структуру либы и ее код хочется выделить 3 оснвных направления работы:
- Модуляризация
- Добавление функциональности
- Отладка и тестирование

Направления расположены в порядке приоритета 🙃

## Modularization

На данный момент все, что связано с сетью представляет из себя по сути монолит. Вся логика сконцентрирована в нескольких классах, что затрудняет:
- Процесс переиспользования
- Процесс добавления функциональности
- Процесс тестирования

В рамках этого направления планируется сделать следующее:
- Вынести политики кэширования в отдельный объект
- Спроектировать и добавть модуль логирования запросов
- Добавить модуль маппинга
- Добавить модуль формирования запроса (например, добавление хедеров)

В итоге, CoreServerRequest будет являться агрегатором большинства модулей. 
В случае, если будет необходимо добавиать функциональность нужно будет просто написать новый модуль и добавить его вызов в CoreServerRequest. 

## More features

После модуляризации останется полным-полно незаполненных дырок. Модулей же нет
Необходимо:
- Переписать существующие политики кэширования и добавить новые
- Добавить модуль, отвечающий за сбор запроса
- Добавить модуль, отвечающий за логгирование
- Пересмотреть работы контекстов и перепроектировать, переработать некоторые куски реализации. 
- Переписать контексты для пагинации. 

## Debugging and testing
Рефакторить.
Тесты писать в процессе работы. 

# Checklist
- [ ] Исправить проблему утечки памяти в пагнириуемых контекстах [#33](https://github.com/surfstudio/CoreNetKit/issues/33)
- [ ] Адаптировать AccessSafe для запросов с пагинацией [#13](https://github.com/surfstudio/CoreNetKit/issues/13)
- [ ] Вынести политики кэширования в отдельный объект [#34](https://github.com/surfstudio/CoreNetKit/issues/34)
- [ ] Доработать/переработать модуль для имплементации логики кэширования (чтение/запись) [#35](https://github.com/surfstudio/CoreNetKit/issues/35)
- [ ] Доработать `CoreServerResponse` [#36](https://github.com/surfstudio/CoreNetKit/issues/36)
- [ ] Добавить модуль маппинга [#37](https://github.com/surfstudio/CoreNetKit/issues/37)
- [ ] Добавить модуль логгирования [#21](https://github.com/surfstudio/CoreNetKit/issues/21) [#27](https://github.com/surfstudio/CoreNetKit/issues/27)
- [ ] Добавить модуль, отвечающий за формирование запроса [#3](https://github.com/surfstudio/CoreNetKit/issues/3)
- [ ] Переработка контекстов. Добавление операций. [#38](https://github.com/surfstudio/CoreNetKit/issues/38)
- [ ] Переработка контекстов для пагинации [#38](https://github.com/surfstudio/CoreNetKit/issues/38)
- [ ] Рефакторинг всего
- [ ] Добавление возможности работать с WebSocket [#40](https://github.com/surfstudio/CoreNetKit/issues/40)
- [ ] Добавление маппинга в Bson [#42](https://github.com/surfstudio/CoreNetKit/issues/42)
- [ ] Добавлние маппинга в Json [#41](https://github.com/surfstudio/CoreNetKit/issues/41)

